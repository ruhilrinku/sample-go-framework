# Application Configuration
database_url=postgres://postgres:password@localhost:5432/item_service?sslmode=disable
database_reader_url=postgres://postgres:password@localhost:5432/item_service?sslmode=disable
database_writer_url=postgres://postgres:password@localhost:5432/item_service?sslmode=disable
grpc_port=50051
http_port=8080

# Migration Configuration
liquibase_changelog=db-migrations/changelog-master.yaml

# FDS IAM Configuration
# Set to the issuer (iss) claim value of FDS-issued JWT tokens.
# When matched, raw FDS identifiers are captured in RequestSession.FDSClaims
# for downstream platform identity resolution.
fds_issuer=https://almpprod.us1.sws.siemens.com/oauth/token
